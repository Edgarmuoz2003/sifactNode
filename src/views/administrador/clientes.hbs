<section>
    <div class="container contenedor-buscar shadow-lg">
        <div class="row">
            <h2 class="mt-2 text-center">Consultar Clientes</h2>
            <div class="col-6 input-buscar">
                <form action="/clientes" method="post">
                    <label for="ID">N° Identificación</label>
                    <input type="text" name="ID" required autocomplete="off">
                    <div class="mi-boton" >
                        <button class="btn btn-secondary" >
                          <i class="bi bi-search"></i> 
                        </button>
                    </div>    
                     {{#if eliminado}} 
                      <p class="text-center">{{eliminado}} </p>
                     {{/if}}            
                </form>
                {{#if error}}
                <p class="text-center">{{error}} </p>
                {{/if}}
            </div>

            <div class="col-12 tabla-usuarios">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Identificación</th>
                            <th>Razon Social</th>
                            <th>Direccion</th>
                            <th>Telefono</th>
                            <th>acciones</th>
                        </tr>                       
                    </thead>
                    
                    <tbody>                                            
                        {{#if data}}
                        <tr>
                            <td>{{data.ID}}</td>
                            <td>{{data.RAZON-SOCIAL}}</td>
                            <td>{{data.DIRECCION}}</td>
                            <td>{{data.TELEFONO}}</td>
                            <td>
                                <a href="/clientes/eliminar/{{data.ID}}" >
                                    <span class="bi bi-trash"></span> <!-- Icono de eliminar -->
                                </a>
                                <a href="/clientes/modificar/{{data.ID}}" >
                                    <span class="bi bi-pencil"></span> <!-- Icono de modificar -->
                                </a>
                            </td>
                        </tr>
                         {{/if}}                        
                    </tbody>
                </table>
                {{#if mensaje}}
                <p class="text-center">{{mensaje}} </p>
                {{/if}}
            </div>
        </div>
    </div>
</section>